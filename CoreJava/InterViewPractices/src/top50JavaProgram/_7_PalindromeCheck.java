package top50JavaProgram;
/*
 * A palindrome string is one whose reverse is also the same string. so we can reverse the input string and check if both
 * String are equal or not. we can also use the String charAt(int index) menthod to check for palindrome Stirng.
 */
public class _7_PalindromeCheck 
{
//	boolean checkPalindromeString(String input)
//	{
//		boolean result=true;
//		int length =input.length();
//		for (int i=0; i<length/2;i++)
//		{
//			if (input.charAt(i) != input.charAt(length-i-1))
//			{
//				result =false;
//				break;
//			}
//		}
//		return result;
//		
//	}
}