package StreamAPI;

import java.util.stream.Stream;

public class GetMiniMaxElements {
	public static void main(String[] args) {
		
		Stream<Integer> list = Stream.of(112, 52, 45, 78, 44, 13, 10);
//		System.out.println("The minimum element are : "+list.min((x,y)->x.compareTo(y)).get());  //for minimum value
		
//		System.out.println("The Maximum element are : "+list.max((x,y)->x.compareTo(y)).get());	//for maximum value
		
		
		
	}
}
